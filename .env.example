# Skopiuj do `.env` w katalogu projektu (`cp .env.example .env`) i uzupełnij.
# Docker Compose wczytuje `.env` automatycznie.

OPENAI_API_KEY=

# Ten sam sekret w `api` i `streamlit` — włącza POST /reseed i przycisk w UI.
RESEED_TOKEN=
